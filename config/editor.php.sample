<?php

/*
 * Here you can specify the editor (like the EDITOR environment variable
 * on *nix) to use when creating a new migration (ladder.php create <name>)
 */
$config = array(
	// Either a full path, or something on the PATH.
	'editor' => 'gvim',

	// Automatically open new migrations when created?
	'auto-edit' => TRUE,
);