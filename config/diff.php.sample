<?php

$config = array(
	// Add tables here to exclude them from diff checks.
	'ignore-tables' => array(
		// 'table_name',
		// 'table_name',
		// ...
	),

	// Should we attempt to compare data?
	'diff-data' => TRUE,

	/**
	 * Alternatively, specify individual tables like so:
	 *
	 * 'diff-data' => array(
	 *		'*' => TRUE,
	 *		'table_name' => FALSE,
	 *		'table_name' => TRUE,
	 * ),
	 */
);